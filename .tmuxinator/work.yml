name: work
#root: ~

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Project hooks
# Runs on project start, always
# on_project_start: command
# Run on project start, the first time
# on_project_first_start: command
# Run on project start, after the first time
# on_project_restart: command
# Run on project exit ( detaching from tmux session )
# on_project_exit: command
# Run on project stop
# on_project_stop: command

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
#pre_window: zsh

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
#tmux_options: -f ~/.tmux.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
#startup_window: editor

# Specifies (by index) which pane of the specified window will be selected on project startup. If not set, the first pane is used.

# Controls whether the tmux session should be attached to automatically. Defaults to true.
# attach: false

# Runs after everything. Use it to attach to tmux with custom options etc.
# post: tmux -CC attach -t default

windows:
  - work:
      layout: f8e8,315x75,0,0[315x46,0,0{158x46,0,0,0,156x46,159,0,8},315x28,0,47{158x28,0,47,9,156x28,159,47[156x21,159,47,10,156x3,159,69,37,156x2,159,73,11]}]
      panes:
        - - cd Work
        - - cd Work
        - - cd Work
        - - cd Work
        -
        - caffeinate

  - ld4-new-setup:
      layout: tiled
      panes:
        - wait_for_ssh ld4-lbl-01
        - wait_for_ssh ld4-lbl-02
        - wait_for_ssh ld4-ret-01
        - wait_for_ssh ld4-ret-02
        - wait_for_ssh ld4-ret-03
        - wait_for_ssh ld4-ret-04
        - wait_for_ssh ld4-hft-01
        - wait_for_ssh ld4-hyb-01
        - wait_for_ssh ld4-mas-01
        - wait_for_ssh ld4-mas-02

  - ld4-misc:
      layout: tiled
      panes:
        - wait_for_ssh ld4-git-01
        - wait_for_ssh ld4-pup-01
        - wait_for_ssh ld4-mon-01
        - wait_for_ssh ld4-nas-01
        - wait_for_ssh ld4-sys-01
        - wait_for_ssh ld4-sys-02

  - zh4-prod:
      layout: tiled
      panes:
        - wait_for_ssh zh4-lbl-01
        - wait_for_ssh zh4-lbl-02
        - wait_for_ssh zh4-mas-01
        - wait_for_ssh zh4-ret-01
        - wait_for_ssh zh4-ret-02
        - wait_for_ssh zh4-hft-01


  - ld4-dev:
      layout: tiled
      panes:
        - wait_for_ssh ld4-lbl-01-dev
        - wait_for_ssh ld4-ret-01-dev
        - wait_for_ssh ld4-hft-01-dev
        - wait_for_ssh ld4-mas-01-dev

  - history:
      layout: tiled
      panes:
        - wait_for_ssh ld4-his-01
        - wait_for_ssh ld4-his-02
        - wait_for_ssh ld4-his-03
  - history-lbl:
      layout: tiled
      panes:
        - wait_for_ssh ld4-hlb-01
        - wait_for_ssh ld4-hlb-02
  - history-dev:
      layout: tiled
      panes:
        - wait_for_ssh ld4-his-01-dev
        - wait_for_ssh ld4-his-02-dev
        - wait_for_ssh ld4-his-03-dev
  - stream:
      layout: tiled
      panes:
        - wait_for_ssh ld4-str-01
        - wait_for_ssh ld4-str-02
        - wait_for_ssh ld4-str-03
        - wait_for_ssh ld4-str-04
